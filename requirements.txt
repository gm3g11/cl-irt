# PUDF: Probabilistic Utility-based Difficulty Function for Curriculum Learning
# Core dependencies for reproducing paper results

# Deep Learning
torch>=2.0.0
numpy>=1.24.0

# Hugging Face ecosystem
transformers>=4.36.0
datasets>=2.14.0
evaluate>=0.4.0
accelerate>=0.24.0
peft>=0.6.0  # For QLoRA/LoRA fine-tuning
huggingface_hub>=0.19.0

# Quantization (for 7B+ models)
bitsandbytes>=0.41.0

# IRT difficulty estimation
py-irt>=0.5.0  # Variational inference IRT
scipy>=1.10.0  # For Nelder-Mead MLE optimization

# Data processing
pandas>=2.0.0
tqdm>=4.65.0

# Optional: for API-based model testing (GSM8K)
# replicate>=0.15.0  # Uncomment if using Replicate API
